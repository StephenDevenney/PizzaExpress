using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using Pizza.Express.Context;
using Pizza.Express.Context.Interfaces;

namespace Pizza.Express.UnitTest.Basket
{
    [TestClass]
    public class BasketContextTest
    {
        [TestMethod]
        public void BasketContext_Constructor()
        {
            //BasketContext testObject = new BasketContext(new Mock<IGlobals>().Object, new Mock<SqlContext>().Object);
            //Assert.IsNotNull(testObject);
        }
    }
}
